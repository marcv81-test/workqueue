---

- name: Read if node bootstrapped already
  stat:
    path: /var/lib/cassandra/.bootstrapped
  register: cassandra_bootstrapped
  changed_when: False

- name: Node is ready for bootstrapping
  include: pre_bootstrap_2.yml
  when: not cassandra_bootstrapped.stat.exists
